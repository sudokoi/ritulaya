/**
 * GitHub OAuth App client ID for the device flow.
 *
 * Client IDs are public identifiers, not secrets. Override at build time
 * with EXPO_PUBLIC_GITHUB_OAUTH_CLIENT_ID if needed.
 */
export const GITHUB_OAUTH_CLIENT_ID =
  process.env.EXPO_PUBLIC_GITHUB_OAUTH_CLIENT_ID ?? "Ov23liSASKMyJ8UvL9LP"