import { View, Text } from "react-native"

export default function SettingsScreen() {
  return (
    <View className="flex-1 items-center justify-center bg-[var(--bg-primary)]">
      <Text className="text-lg text-[var(--text-muted)]">Settings</Text>
    </View>
  )
}
